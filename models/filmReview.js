export default class FilmReview {
    constructor(author,authorRole,text,rate,filmId) {
        this.author = author;
        this.authorRole = authorRole;
        this.text = text;
        this.rate = rate;
        this.filmId = filmId;
    }
}